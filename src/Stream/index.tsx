import React from 'react';
import {
  View,
} from 'react-native';

declare const global: {HermesInternal: null | {}};

const StreamScreen = () => {
  return (
    <View />
  );
};

export default StreamScreen;
