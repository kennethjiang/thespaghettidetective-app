import React from 'react';
import {
  View,
} from 'react-native';

declare const global: {HermesInternal: null | {}};

const LogInScreen = () => {
  return (
    <View />
  );
};

export default LogInScreen;
